# 사진 폴더

첫 화면 소개 글 옆에 들어가는 사진을 이 폴더에 넣습니다.

- 파일명은 `_config.yml` 의 `lab.photo` 값과 맞춰야 합니다 (기본값: `home.jpg`).
- 파일이 없으면 그림 영역이 자동으로 사라지고 소개 글이 전체 폭을 씁니다.
- 권장: 가로형(4:3 또는 3:2), 가로 1200px 이상, .jpg

사진 아래에 설명을 넣으려면 `_config.yml` 의 `lab.photo_caption` 에 문구를 적으세요.
